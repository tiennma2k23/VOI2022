#include <bits/stdc++.h>
using namespace std;
const int maxn = 1e5 + 1;
int n;
int a[maxn], b[maxn], c[maxn], d[maxn];
int32_t main()
{
    cin >> n;
    for (int i = 0; i < n; i++)
        cin >> a[i] >> b[i] >> c[i] >> d[i];
}