#include <bits/stdc++.h>
using namespace std;
int32_t main(int argc, char const *argv[])
{
    /* code */
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    for (int i = 1; i <= n; i++)
        cin >> b[i];
    return 0;
}
